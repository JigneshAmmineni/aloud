# GCP project — aloud-498522
# ADC credentials are mounted automatically from the host in docker-compose.yml.
# These values are already set in docker-compose.yml; copy to .env only if you need to override.

# GOOGLE_CLOUD_PROJECT=aloud-498522
# GOOGLE_CLOUD_LOCATION=global

# Optional: override the Gemini Live model
# GEMINI_MODEL=gemini-2.0-flash-live-preview-04-09
